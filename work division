Person 1 — HTML Structure

index.html likhna: teeno screens (start, quiz, result) ka structure
Saare elements ko sahi IDs dena
Semantic tags use karna (header, footer, etc.)

Person 2 — CSS Styling (Layout)

Container, header, buttons, screens ki styling
Colors, spacing, fonts decide karna
style.css ka upper half

Person 3 — CSS Styling (Responsive + Components)

Options, progress bar, timer warning, result box ki styling
Mobile responsive media query likhna aur test karna
style.css ka lower half

Person 4 — JavaScript Core Logic

Quiz start karna, question load karna (startQuiz(), loadQuestion())
Option select karna, answer lock karna, green/red highlight (selectOption())
Next/Previous button logic

Person 5 — JavaScript Timer + Result

30 second timer (startTimer())
Score calculation, percentage, result screen (showResult())
localStorage high score logic
Restart logic

Person 6 — Question Bank + Documentation + Testing

Saare 56 questions likhna/verify karna (sabhi 7 subjects se)
README.md aur viva questions document banana
Pura project test karna — sab buttons, edge cases, mobile pe check karna
Bugs report karna baaki 5 logo ko